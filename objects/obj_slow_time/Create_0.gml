slow_multiplier = ult_slow_get_speed_multiplier(level)
range = ult_slow_get_radius(level)
starting_duration = ult_slow_get_duration(level)
alarm[0] = starting_duration

ultimate_initialize(self)
ult_type = ULTIMATE_SLOW